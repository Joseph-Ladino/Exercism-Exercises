#if !defined(HEXADECIMAL_H)
#define HEXADECIMAL_H
#include <cmath>
#include <string>

namespace hexadecimal {

	unsigned int convert(std::string);

}  // namespace hexadecimal

#endif // HEXADECIMAL_H